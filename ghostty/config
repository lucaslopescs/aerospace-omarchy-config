# Ghostty config — tuned for an AeroSpace tiling workflow.
#
# cmd+t uses native macOS tabs (default). AeroSpace auto-floats Ghostty
# windows (see aerospace.toml) so the invisible tab-grouped NSWindows
# don't get tiled.
